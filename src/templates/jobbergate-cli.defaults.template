ARMADA_API_BASE={backend-base-url}
SENTRY_DSN={sentry-dsn}
SENTRY_ENV={sentry-env}
JOBBERGATE_S3_LOG_BUCKET={s3-log-bucket}
JOBBERGATE_AWS_ACCESS_KEY_ID={aws-access-key-id}
JOBBERGATE_AWS_SECRET_ACCESS_KEY={aws-secret-access-key}
OIDC_DOMAIN={oidc-domain}
OIDC_AUDIENCE={oidc-audience}
OIDC_CLIENT_ID={oidc-client-id}
JOBBERGATE_COMPATIBILITY_MODE={compatibility-mode}
DEFAULT_CLUSTER_NAME={default-cluster-name}
