upgrade:
  description: >
    Upgrade new-jobbergate-cli.
  params:
    version:
      type: string
      description: Version of new-jobbergate-cli to upgrade to.
  required:
    - version

show-version:
  description: >
    Display the version and information about new-jobbergate-cli.
